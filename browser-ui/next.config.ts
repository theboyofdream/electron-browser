import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    // devIndicators: false,
    output: "export",
    assetPrefix: ".",
    // basePath: "/views",
    // assetPrefix: "/views",
};

export default nextConfig;
