{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "install": { "cache": false },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
